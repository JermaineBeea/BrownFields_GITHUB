PRAGMA foreign_keys=OFF;
BEGIN TRANSACTION;
CREATE TABLE world (id INTEGER PRIMARY KEY CHECK (id = 1), width INTEGER NOT NULL, height INTEGER NOT NULL);
INSERT INTO world VALUES(1,5,5);
CREATE TABLE Obstacles (id INTEGER PRIMARY KEY AUTOINCREMENT, X INTEGER NOT NULL, Y INTEGER NOT NULL, width INTEGER NOT NULL, height INTEGER NOT NULL);
INSERT INTO Obstacles VALUES(1,1,1,2,1);
INSERT INTO Obstacles VALUES(2,3,2,1,3);
DELETE FROM sqlite_sequence;
INSERT INTO sqlite_sequence VALUES('Obstacles',2);
COMMIT;
